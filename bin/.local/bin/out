#!/bin/sh

options="Close X\nShutdown\nRestart\nLock"
choice=$(echo "$options" | dmenu -p 'What would you like to do?')

case $choice in
    "Restart")
        sudo reboot now;;
    "Shutdown")
        sudo poweroff now;;
    "Lock")
        slock ;;
    *) ;;
esac
